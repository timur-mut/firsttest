// Areas (rooms) layer — STUB owned by Unit 4. Renders filled room polygons +
// area labels once detectAreas() is implemented. Sits at the back (z-order).

export function AreasLayer() {
  return null;
}
